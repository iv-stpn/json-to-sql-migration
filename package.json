{
	"name": "json-to-sql-migration",
	"type": "module",
	"version": "2.1.2",
	"description": "A TypeScript library that converts JSON-based data schemas into SQL migrations",
	"keywords": [
		"sql",
		"json",
		"sql-migration",
		"typescript",
		"database",
		"zod"
	],
	"homepage": "https://github.com/iv-stpn/json-to-sql-migration#readme",
	"repository": {
		"type": "git",
		"url": "https://github.com/iv-stpn/json-to-sql-migration.git"
	},
	"bugs": {
		"url": "https://github.com/iv-stpn/json-to-sql-migration/issues"
	},
	"main": "./dist/index.js",
	"module": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"sideEffects": false,
	"scripts": {
		"build": "bun run clean && bun run build:lib && bun run build:types",
		"build:lib": "bun build src/index.ts --outdir dist --format esm --target node",
		"build:types": "tsc --project tsconfig.build.json",
		"clean": "rm -rf dist",
		"pretest": "bun run scripts/check-postgres.ts",
		"test": "bun test test/migration.test.ts",
		"test:integration": "bun run pretest && bun test test/integration.test.ts",
		"test:all": "bun run pretest && bun test",
		"test:watch": "bun test --watch",
		"typecheck": "tsc --noEmit --pretty",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"changeset": "changeset",
		"changeset:version": "changeset version",
		"changeset:publish": "changeset publish",
		"release": "bun run build && bun run changeset:publish",
		"prepublishOnly": "bun run build"
	},
	"files": [
		"dist",
		"README.md",
		"LICENSE"
	],
	"license": "MIT",
	"author": "Ivan Stepanian <iv.stpn@gmail.com>",
	"dependencies": {
		"json-to-sql-parser": "^4.0.2",
		"zod": "^4"
	},
	"devDependencies": {
		"@biomejs/biome": "2.1.4",
		"@changesets/changelog-github": "^0.5",
		"@changesets/cli": "^2",
		"@types/bun": "latest",
		"@types/node": "^20",
		"@types/pg": "^8.15.0",
		"pg": "^8.16.0",
		"typescript": "^5"
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"publishConfig": {
		"access": "public"
	}
}
